[project]
name = "wealthmap"
version = "1.0.0"
description = "Local web app for backtesting personal portfolios"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24",
    "requests>=2.28",
]

[tool.vercel]
entrypoint = "api.app:app"
